---
layout: base.njk
---
{% from "entry.njk" import entry %}
<article class="project">
  <a class="back" href="{{ '/' | url }}">← All projects</a>

  {{ entry({
    title: title,
    status: status,
    oneliner: oneliner,
    tech: tech,
    problem: problem,
    built: built,
    impact: impact,
    role: role,
    link: link
  }) }}

  {{ content | safe }}
</article>
