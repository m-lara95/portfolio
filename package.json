{
  "name": "portfolio",
  "version": "0.1.0",
  "description": "A clean, genericized static portfolio site of my projects.",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "scan": "node scripts/scan.mjs",
    "secret-scan": "node scripts/secret-scan.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
